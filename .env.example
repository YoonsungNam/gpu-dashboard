# 데이터 소스 토글 (HANDOFF.md §6) — 복사해서 .env 로 쓰세요 (.env는 gitignore됨)
#
# 'true'(기본): mock standalone — 디자이너/오프라인 실행, 백엔드 불필요
# 'false':     실 백엔드 API(/board/api) 호출 — 사내 빌드는 이 값으로 주입
VITE_USE_MOCK=true

# 실 API 베이스 경로. dev는 vite.config.ts proxy, prod는 nginx가 라우팅한다.
VITE_API_BASE=/board/api
